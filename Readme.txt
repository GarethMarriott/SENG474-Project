###
Gareth Marriott (V00875748)
Roxanne Dewing (V00853942)
###

Instructions (bash shell):

1. have access to python 3 maped to the "python" command in the shell
2. Type in the project_directory$ sh RUN.sh
3. The pyhon file will gather data from NFL.com apis and store the data in text files
4. Once all the data is collected the Perceptron will use the data stored locally to run the algorithm

the results will be printed to the console

You can re-run the model by the command
python perceptron.py
